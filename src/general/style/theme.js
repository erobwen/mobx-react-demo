
import { createTheme } from '@mui/material/styles';

export const loginTheme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: 'rgb(81, 21, 135)',
    },
    secondary: {
      main: '#b18f32',
    },
  }
});